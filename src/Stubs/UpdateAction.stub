<?php

namespace $Namespace$;

use App\$Model$;
use Slexx\LaravelActions\Action;

class $Class$ extends Action
{
    /**
     * Determine if the user is authorized to execute this action.
     *
     * @param int $id
     * @param array $data
     * @return bool
     */
    public function authorize(int $id, array $data)
    {
        return true;
    }

    /**
     * @param int $id
     * @param array $data
     * @return $Model$
     */
    public function execute(int $id, array $data)
    {
        $$model$ = $Model$::findOrFail($id);

        $$model$->update($data);

        return $$model$;
    }
}
